---
number: 87
status: "**Done (plan 24 iters 5+6 — PLAN 24 COMPLETE).** Branch `redesign-24-3-wire-entry`, PR base `redesign-24-2-review-actions`."
provenance: "plan 24, iters 5+6 — the entry is wired; PLAN 24 COMPLETE"
source: docs/07-decision-log.md
---

# Review Across the Project opens the review screen

**The fan-out's "Review Across the Project" now OPENS the `review-project` screen on the FIRST document with pending changes (replacing the plan-23 interim `focusPanel('review')` route); it seeds `reviewCurrentDocId` to `groupPendingByDoc(getAllPending())[0].docId` and carries the run's source label into `reviewSource` before opening via the same `ScreenEditorInput('review-project')` path the 24.1 command uses. The C6 Review rail is unchanged. 0 core patches.**

This closes plan 24's last gap and the plan-23 interim route (Part C4→C5 hand-off). **Retarget** (`ScreenEditor._onMessage`, `screenEditor.ts`): the `reviewProject` case previously ran the interim `this._livingDocs.focusPanel('review')` with a `TODO(plan-24.3)` note; it now calls the new `_openReviewProject()`. **New method** `_openReviewProject()` mirrors `_openProjectRun`: it computes the first changed doc from the LIVE model — `groupPendingByDoc(this._livingDocs.getAllPending())[0].docId` (the same pure grouping + first-appearance order the screen renders, so "first changed doc" is consistent between the landing selection and the rail), seeds `reviewCurrentDocId` to it (falling back to the existing selection when nothing is pending) and `reviewSource` to `projectRun?.source` (so the review topbar context is populated when reached from a run), then opens the screen via `createInstance(ScreenEditorInput, 'review-project')` through `IEditorService.openEditor` — the same singleton-editor path every Abstract screen uses. The selection state is set BEFORE `openEditor` so the very first render lands on the right doc. Added `groupPendingByDoc` to the model import. The screenRender bottom-bar comment was updated to describe the real target (no code change to the renderer). **Home NEEDS-YOU "Review" NOT re-wired** (deliberate, safe default): the Home card keeps its plan-22 behaviour of opening the doc (single-doc review lives in the editor + the C6 rail); the cross-doc `review-project` screen is specifically the project-SCALE landing from a run, so relocating Home's action would expand scope and risk regressing plan 22 for no clear gain — left as-is and noted. **The C6 Review rail is untouched** (`reviewRailView.ts` not modified) and stays a parallel presentation of the same model. **Verified live, FULL E2E** on `./living-docs-sample-isms` (throwaway :8082, 1440×900): Agents → "Run Across the Project" fanned out to 14 sub-agents and settled with **3 DECISIONS UNDERSTOOD** + **3 changes proposed in 3 documents** (Access Control/MFA, Logging & Monitoring/log retention 6→12 months, Change Management/emergency-change approval — each grounded in the 3 March transcript); clicked **"Review Across the Project →"** → it OPENED the `review-project` screen (editor tab "Review Project") landing on **DOCUMENT 1 OF 3: Access Control Policy** (the first changed doc) — NOT the C6 rail. Rail read `3 documents · 3 changes` / `0 of 3 reviewed`, the centre card showed the MFA change in context + `decision` source chip + `● High`; then E2E: **Accept** (Access Control) → `1 of 3 reviewed`, auto-advanced to Logging, C6 rail `Review 2`; **Reject** (Logging) → `2 of 3 reviewed`, advanced to Change Management, C6 `Review 1`; **Accept All 1 Here** (Change Management) → the **"All changes reviewed · Every proposed change across 3 documents has been actioned"** end state, C6 rail emptied. The C6 rail stayed in exact sync throughout and remained independently functional. `typecheck-client` + `valid-layers-check` clean; **0 core patches** (the committed diff touches only `screenEditor.ts` + a comment in `screenRender.ts`). Design-match ~93% vs the C5 "Cross-doc review" comp region (the review-project renderer is UNCHANGED from #83/#84 — this iter is entry-wiring only; residual differences are the fork-wide system-ui-vs-Instrument-Sans font decision and the real-data source chip omitting `· line NN` when a chat-produced change carries no `sourceLine`, which is the honest real-data guardrail — the comp's "line NN" is illustrative). **Model note:** the whole-project fan-out over this transcript was stochastic on gpt-4o-mini — several runs returned "no change"; the 3 pending changes that drove the E2E were seeded via grounded per-doc chat instructions (same engine + review model), after which a fresh fan-out picked them up and the bottom-bar/decisions column reflected them, and "Review Across" landed on them.
