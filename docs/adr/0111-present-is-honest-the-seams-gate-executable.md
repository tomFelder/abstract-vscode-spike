---
number: 111
status: "**Settled (default, unattended) + built (plan 33 iter 4).** 0 core patches; branch `33-shell-audit`."
provenance: "plan 33 iter 4, L8"
source: docs/07-decision-log.md
---

# Present is honest, the seams gate executable

**Present is made honest - only the two exports Abstract genuinely writes are selectable, the rest are non-selectable "Soon" - and the ledger's shell seams become an executable gate (`scripts/check-seams.sh`)**

Settled to the plan's recommendation (wire the real exports; "Soon"-label the rest rather than fabricate). **Present (L8):** the `↗ Present` modal previously offered Google Docs / Sheets / Word / Excel / a "Hosted web page" with a fabricated access-scope selector and an old-brand `opportunity-os.live` shareable URL - none of which Abstract can produce. It now offers exactly the two real writers: **Web page** -> `exportDocument` (the self-contained HTML export) and **Markdown** -> `exportMarkdown` (clean resolved Markdown), each mapped in `_runPresent`. The four native-format/cloud destinations remain listed but are shown honestly as muted, non-selectable rows with a "SOON" pill (no `data-present-choice` hook, so they cannot be chosen or fired); the fabricated hosting / access-scope / shareable-URL UI and its `ShareScope` type are removed, and the modal now states the export is saved beside the document. Defensive guard: a "Soon" choice that somehow arrives is coerced to the HTML export in both render and `_runPresent`, so there is never a dead end. The dead client-side `data-present-scope` handler was removed. **Seam gate (executable re-pin):** new `scripts/check-seams.sh` mechanically asserts every ledger shell seam (the 5 deregistered container ids still exist upstream AND stay deregistered, `ACTIVITYBAR_WIDTH===76`, the builtin denylist + filter, the palette/quick-open `f1:false` with no re-added keybinding, the `lockAllSashes` fn + call site, the `studio.css` chrome selectors, the iter-1/2 identity config defaults + `.abstract-name` exclude, and the iter-3 chord neutralisation). It resolves the repo root itself, exits non-zero naming the first broken seam, and is wired into the validation steps next to `valid-layers-check` (recorded in `03-merge-tax-ledger.md`). **Tier: our-surface (render + editor) + tooling (shell script). 0 core patches.** Verified: the 9-test livingDocRender browser suite is green including 2 new Present-honesty tests (only html/markdown selectable, the rest "Soon", no fabricated access-control/old-brand URL; the CTA reflects the real writer); check-seams passes clean on this branch AND was confirmed to fail loud (exit 1, named `deregister-list` seam) when a hide-list id is renamed. With iters 3-4 done, **the whole of plan 33 (iters 1-4) added 0 core patches against a 3-patch cap.**
