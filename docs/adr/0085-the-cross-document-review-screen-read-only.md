---
number: 85
status: "**Done (plan 24 iters 1+2, read-only).** Branch `redesign-24-1-review-screen`, PR base `redesign-23-4-decisions`."
provenance: "plan 24, iters 1+2 merged — the cross-document review screen, READ-ONLY"
source: docs/07-decision-log.md
---

# The cross-document review screen, read-only

**`review-project` is a new `ScreenId` rendered by the EXISTING `ScreenEditor` webview (no new editor), a second presentation of the SAME review model the C6 rail consumes; the 292px doc-nav rail + centre change cards are built from real pending; per-change Accept/Tweak/Reject + the batch bar are RENDERED but inert (TODO 24.2). 0 core patches.**

This iteration merges plan-24 iters 1 (screen scaffold + 292px rail + settle D24-A) and 2 (centre change cards, read-only). **Screen registration (mirrors plan 23's `project-run`):** added `'review-project'` to the `ScreenId` union + a `renderReviewProject(state)` in `screenRender.ts` (48px topbar + `<div style=flex:1;display:flex>` holding the rail + centre column), a `switch` case in `renderScreenHtml`, and `TITLES['review-project'] = 'Review Project'` in `screenEditorInput.ts`. Opened via the SAME singleton-editor path every screen uses (`createInstance(ScreenEditorInput, 'review-project')` through `IEditorService.openEditor`); a palette command `livingDocs.open.review-project` ("Abstract: Review Across the Project") is registered in `livingDocs.contribution.ts` for reach (the real in-product entry from the fan-out is plan 24.3). **Data (reuse the engine):** `ScreenEditor._reviewProjectState()` reads `getAllPending()` live each render (the SAME model `reviewRailView.ts` consumes — not re-derived); the renderer groups it with the new pure `groupPendingByDoc(pending)` helper (TDD, 2 tests: first-appearance order + empty) for both the rail rows and the centre cards. Only two bits are local screen state: `reviewCurrentDocId` (the doc shown in the centre — set by the `reviewDoc` message when a rail row is clicked, pure screen navigation, not an engine action) and `reviewReviewedDocIds` (docs emptied this session → the `✓` glyph; empty this read-only iter). **Rail (C5, 292px):** header `N documents · M changes`, a green (`ok`, `oklch(0.6 0.13 150)`) progress bar, `X of N reviewed`, then one row per changed doc with a `✓ reviewed` / `● current` (accent tint `#eef0fb` + 3px accent left bar) / `○ pending` glyph + mono count. **Centre cards (C5):** `DOCUMENT n OF m` mono eyebrow, doc title (H1 30/600), `N changes proposed · review each in context`, then one card per change showing the change in context (addition `#e9f6ee/#2c8159`, removal struck `#fbeeee/#b5514b` — an insert renders as pure additions), a `decision · line NN` source chip (from `sourceQuote`/`sourceLine`, line OMITTED when unknown — never fabricated), and the D24-A confidence chip (`● High` ok tokens / `◐ Inferred · needs your eyes` attention tokens `#8A6D1A`/`#FDFAF2`/`#E4DCCB`, with the whole card attention-tinted). **READ-ONLY guardrail:** Accept / Tweak / Reject per card + `Accept All N Here` / `Next: <doc> →` (dark) / `Accept All Remaining (N)` are RENDERED (so the layout matches the comp) but inert (`cursor:default`, no handler) with `// TODO(24.2)` markers noting the wiring (Accept→`approve(id)`, Reject→`reject(id)`, Tweak→`focusChange` navigate, Accept-all→`approveAll(docId)`, Next→advance current). Tokens verbatim Part B; title-case buttons; reading type stays UI sans (handoff 4b — the systematic serif departure, consistent with plans 23/22). **Verified live** on `./living-docs-sample-isms` (throwaway :8082, 1440×900): a whole-project chat fan-out (`@Security Review - 3 Mar.txt`) produced **3 real pending changes across 3 docs** (Access Control/MFA, Logging & Monitoring/retention, Change Management/emergency changes); opened `review-project` via the palette command; the 292px rail showed `3 documents · 3 changes` + progress bar + `0 of 3 reviewed` + the three docs with `●`/`○` + real counts; the centre showed `DOCUMENT 1 OF 3` Access Control with the change in context, a real `decision · line 2` chip, a `● High` chip (0.85 ≥ 0.8), and the inert action row; clicking Change Management in the rail switched the centre to `DOCUMENT 3 OF 3` with `decision · line 30` — local navigation works. Cross-checked against the C6 Review rail: both read 3 changes across 3 docs (the rail is untouched and still fully works). `typecheck-client` + `valid-layers-check` clean; 17 LivingDoc model tests pass (2 new suites); **0 core patches** (two temporary message-handler repoints — `reviewProject`/`openFirstDoc` → open review-project — were used ONLY to reach the screen during verification when the chrome-devtools synthetic keydown could not trigger VS Code's keybinding; both fully reverted, and the palette command ultimately drove verification via Quick Open's typed `>` input — the committed diff touches only `livingDocsModel.ts` / `screenRender.ts` / `screenEditor.ts` / `screenEditorInput.ts` / `livingDocs.contribution.ts` + tests). Design-match ~93% on the in-scope surface (rail + cards + chips; actions are 24.2).
