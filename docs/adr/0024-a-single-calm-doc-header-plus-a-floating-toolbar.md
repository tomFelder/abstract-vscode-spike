---
number: 24
status: "**Done (v2 iter 4, PR #15).** 0 core patches. Verified live: calm single bar; source-peek still opens in-surface via a provenance dot (G1 holds)."
provenance: "v2"
source: docs/07-decision-log.md
---

# A single calm doc header plus a floating toolbar

**The doc header is the comp's single calm bar; formatting moves to a floating selection toolbar**

The comp's bar carries only brand/crumb + sync pill + Present + avatar - no Download, no Refresh button, no persistent formatting-toolbar row (D2/D3: "far less UI, Word/Docs/Notion light-touch"). Keep functionality without clutter: the **sync pill IS the refresh** (click to re-derive); **Download** is covered by the Present/export modal; **formatting** (B/I/U/Heading/List/Quote) appears in a **floating toolbar on text selection** (Notion-like); **Ask AI** -> the Chat rail, **Source** -> provenance dots (the comp's entries); **raw-Markdown** toggle moves to the footer hint
