---
number: 83
status: "**Done (plan 23 iter 4 — plan 23 COMPLETE).** Branch `redesign-23-4-decisions`, PR base `redesign-23-3-swarm-grid`."
provenance: "plan 23, iter 4 — completes plan 23"
source: docs/07-decision-log.md
---

# Decisions understood come from real source grounding

**The C4 decisions-understood column is built from a REAL per-change SOURCE GROUNDING: the fan-out model returns a verbatim `sourceQuote` + `sourceLine` per edit, the parser reads them, changes group into decisions, and the column shows each decision with its transcript line + N-docs-affected — with a truthful degrade when the model omits grounding. 0 core patches.**

This closes the last gap from decision #77 (chat edits carried only free-text `rationale` + `sourceCells: []`, no source line). **Model schema + prompt** (`_chatRespondMulti`, `livingDocsService.ts`): the JSON schema now asks each edit/insert for `"sourceQuote"` (a short VERBATIM sentence from the attached source the change implements) + `"sourceLine"` (its 1-based line, if the source shows numbered lines), and the system prompt instructs the model to GROUND every change in a specific decision line. **Parser (TDD, `livingDocMarkdown.ts`):** `parseMultiChatResponse` now normalises each edit/insert via `normaliseEdit`/`normaliseInsert`, copying through only the fields the model supplied and attaching `sourceQuote`/`sourceLine` when present; a missing field, or a non-numeric `sourceLine`, degrades to `undefined` with NO fabricated keys (8 parser tests, incl. grounding-present-on-edit, grounding-on-insert, absent-degrade, non-numeric-line-dropped-quote-kept). New pure `findQuoteLine(sourceText, quote)` looks up a quote's TRUE line in the real source (whitespace/case-insensitive, tolerant of a source that wraps a decision across lines + prints its own line-number column) and returns `undefined` when not found — NEVER guesses (3 tests). **`IProposedChange`** gains optional `sourceQuote?`/`sourceLine?` (`livingDocsModel.ts`); `_queueChatEdit`/`_queueChatInsert` populate them via `_resolveSourceGrounding` — keep the model's quote; take the model's line if given, else the verified `findQuoteLine` lookup, else leave the line undefined (quote only). **Grouping (TDD):** pure `groupDecisions(pending)` → `IDecisionGroup[]` groups changes by source line, else quote, else — when no change in the run is grounded — honestly by `rationale` (`grounded:false` so the card omits the line chip); `docsAffected` counts DISTINCT docs (a doc with several changes from one decision counts once); first-appearance order (5 tests). **Render (`decisionsRail`, `screenRender.ts`):** real cards matching the comp — a `N decisions understood` mono/accent header, then per decision a `Security Review - 3 Mar.txt · line N` mono chip on top, the decision quote in reading type, and `→ N documents affected` in accent (`#4650b8`, 600); truthful empty / in-flight / degraded states. `IProjectRunScreenState.decisions` is computed each render in `ScreenEditor._projectRunState` from `groupDecisions(getAllPending() restricted to this run's doc set)`. **Verified live** on `./living-docs-sample-isms` (throwaway :8082, 1440×900): Agents → "Run Across the Project" → swarm spins 14 tiles then settles; the decisions column populated with REAL decisions each carrying a source line cross-checked against `Security Review - 3 Mar.txt` (MFA→line 2, log retention→line 7, BYOD→line 15, incident 1-hour→line 23, patch cadence→line 27, change approval→line 30); bottom bar + Review rail cross-check exactly. The cheap model (gpt-4o-mini) RELIABLY emitted `sourceLine` this loop; the quote-lookup/degrade path is covered by tests and is the honest fallback (did not need to fire live). 16 new tests; 131 LivingDoc tests pass; `typecheck-client` + `valid-layers-check` clean; **0 core patches** (a temporary workspace-only `activityBar.location` flip was used ONLY to reach the Agents nav for verification and fully reverted; the committed diff touches only `livingDocMarkdown.ts` / `livingDocsModel.ts` / `livingDocsService.ts` / `screenRender.ts` / `screenEditor.ts` + tests). Design-match ~93% on the FULL C4 screen (all four parts now exist).
