---
number: 39
status: "**Decided (v5 iter 1) — Tom's call.** Drives R1 (open-folder action + switch) and R2 (empty state + folder-reflecting Home + resolve \"New project\"). Builds in iter 2+. Note: `listDocuments` currently filters to \"living\" docs only — R2 will broaden discovery to all `.md` with a living/plain flag."
provenance: "v5"
source: docs/07-decision-log.md
---

# The folder is the project

**"The folder IS the project", and the tree/Home show all Markdown with living docs badged**

R1/R2 hinge on what a "project" is once you open a real folder. Tom's call: (a) **the open folder is the project** — Home reflects THIS folder's documents/sources, there is no multi-project dashboard; the demo project cards are removed; the no-op "New project" button becomes **"Open folder…"**. (b) The tree-rail + Home show **all `.md` in the folder** (including plain ones like `Team Notes.md`), with "living" docs (those with `sources:`/`context:`/bind links) **visually badged** — so the surface reflects the real folder honestly rather than hiding plain docs.
