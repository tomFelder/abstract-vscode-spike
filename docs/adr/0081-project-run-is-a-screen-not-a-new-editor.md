---
number: 81
status: "**Done (plan 23 iter 2).** Branch `redesign-23-2-run-screen`, PR base `redesign-23-1-isms-sample`."
provenance: "plan 23, iter 2"
source: docs/07-decision-log.md
---

# Project-run is a screen, not a new editor

**The `project-run` screen is a new `ScreenId` rendered by the existing `ScreenEditor` webview (no new editor); it is reachable live from the Agents screen, renders the command strip, and shows a truthful idle state — with the bottom-bar `Review across the project` routed to the Review rail as the plan-24 interim. 0 core patches.**

Iter 2 builds the reachable, truthful SHELL of C4 (the swarm grid is 23.3, the decisions column is 23.4). **Screen registration:** added `'project-run'` to the `ScreenId` union + a `renderProjectRun(state)` in `screenRender.ts` (its own 48px run topbar + command strip + idle body + 66px bottom bar), a `switch` case in `renderScreenHtml`, and a `TITLES['project-run'] = 'Agent Run'` in `screenEditorInput.ts`. **Reused the open-screen path:** `ScreenEditor._openProjectRun()` opens `createInstance(ScreenEditorInput, 'project-run')` through `IEditorService.openEditor` — the exact singleton-editor path `home`/`templates`/`agents` use (no new editor, no new input type). **Entry point (D23-B):** the Agents-list header gained a primary `✦ Run Across the Project` button (`data-msg="runProject"`) beside a demoted-ghost `＋ New agent`; the message handler calls `_openProjectRun()`. The actual whole-project chat fan-out kick (`addFolderToWorkingSet` + `sendChatMessage` → `_chatRespondMulti`) is left as an explicit `// TODO(23.3)` inside `_openProjectRun` — opening the screen from Agents works live now, landing on the idle state. **Idle-state guardrail:** `IScreenState.projectRun` is `undefined` when no run is active, so the command strip renders a calm "No project run in progress…" prompt and the body renders the honest placeholder ("the swarm and decisions appear here as the run proceeds") + a "Go to Agents" affordance — NEVER the comp's illustrative 38-changes / 24-doc ISMS numbers (plan-17 never-fabricate). Bottom-bar totals are idle 0s (real totals from `summariseProjectRun(docs, getAllPending())` land in 23.3). **Interim route + TODO:** the bottom bar's primary `Review Across the Project →` (`data-msg="reviewProject"`) calls `focusPanel('review')` with a `TODO(plan-24)` to retarget it at the cross-document review surface. Tokens verbatim from Part B; reading type stays UI sans (handoff Part B/F, decision 4b — handoff wins over the comp's Newsreader serif). Verified live on `./living-docs-sample-isms` (throwaway :8082): Agents → `Run Across the Project` opens the screen, command strip + truthful idle render (no fabricated numbers), `Review Across the Project →` focuses the Review rail. Design-match ~92% on the parts that exist. `typecheck-client` + `valid-layers-check` clean; **0 core patches** (a temporary `activityBar.location` flip was used ONLY to reach the icon-nav during verification and fully reverted — the committed diff touches only `screenRender.ts` / `screenEditor.ts` / `screenEditorInput.ts`).
