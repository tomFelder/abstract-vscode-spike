---
number: 93
status: "**Done (plan 25 iter 3, FINAL).** 0 new core patches; branch `redesign-25-3-regression`, PR base `redesign-25-2-nav-chip`."
provenance: "plan 25, iter 3, FINAL"
source: docs/07-decision-log.md
---

# The redesign loop closes with no new core patches

**Regression sweep passes at 76px; both logged design gaps closed in CSS; desktop smoke deferred — plan 25 and the whole redesign loop complete, 0 new core patches**

The final iteration ran the acceptance-gate regression sweep live at 1440x900 on `:8080` and closed the last two design gaps. **Sweep:** Home, Editor, Templates, Knowledge, Agents — and the project-scale agent-run canvas (plans 23/24) — all render intact at the 76px nav; none is squeezed. The active white chip tracks the surface correctly across all five. The **tree-rail and right rail are unaffected by the nav change** (plan 25 never touched the sidebar/aux sizing code — only the activity bar). They render ~252 / ~374 px in the browser; the configured `_pinShellWidths` values are **264 / 392**, and the ~12/18px difference is the modernUI floating-parts inset chrome (consistent ~6/12px gutters between every part, present since the modernUI overrides landed) — a pre-existing shell-chrome artifact, not a plan-25 regression. **Polish (studio.css, appearance-only):** (a) the inactive glyph colour gap `#606060` -> `#868B95` — root cause was `activityBar.css`'s `:not(.checked)` `!important` rule tied at (0,9,0) and winning on source order; adding the real `.composite-bar` ancestor to the studio selector reaches (0,10,0) and wins; (b) the 1px divider after Editor — a `34px x 1px` `#E4E6EA` `::before` on the Templates item (matched by its stable `codicon-living-docs-templates` class), centred via a flex-column-centre item, matching the comp's `margin:4px 0` hairline between Editor and Templates. **Nav design-match: 96%** vs comp C1 (>= 90% gate met); residual ~4% is spec-compliant (Part-B `system-ui` fallback; Part-B e1 shadow which the handoff says WINS over the comp pixels) or diminishing-returns (bespoke SVG glyphs vs codicons). **Desktop real-disk smoke: DEFERRED** (matching decision 71's precedent) — iter 3 changed only appearance CSS, and driving the packaged Electron build is impractical from the browser-bound chrome-devtools session; a 2-minute manual desktop check (`./scripts/code.sh ./living-docs-sample/brief` or the `launch` skill, `TMPDIR=/tmp`) should confirm the 76px labeled nav + active white chip render in the packaged workbench, not only web. **Whole plan-25 stack (iters 1-3): 0 new core patches** (still 5 added total — the 76px `ACTIVITYBAR_WIDTH` seam was paid once in v2 iter 9). This completes Part E row 7 and the entire Abstract UI Redesign loop.
