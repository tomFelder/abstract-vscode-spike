---
number: 125
status: "**Done (plan 28 iter 3).** 0 core patches; branch `28-templates-gen`."
provenance: "plan 28, iter 3, D28-B/D28-C"
source: docs/07-decision-log.md
---

# Generation writes a skeleton, then drafts prose

**Generation is one calm sheet then the EXISTING chat path: a static skeleton written to disk (headings + verbatim bind links; H1 = the document name; slots stripped; `template:` provenance), and the prose drafted as insertion proposals in the review rail - no new approve/apply path, 0 core patches**

Wires the iter-1/2 template model into real generation. **Use Template** now opens the D28-B sheet (document name + one optional free-text note; shared across cards, the opening card carries its template URI) and posts `generateFromTemplate(templateUri, docName, note)`. The service (a) parses the template with the same `parseLivingDoc`, (b) builds the skeleton with a new pure `buildTemplateSkeleton` (`common/livingDocMarkdown.ts`): HTML-comment guidance stripped, `{{slot}}` runs stripped, the first H1 set to the document name, every line carrying a `bind:` link copied through VERBATIM so the draft is born bound, and a frontmatter recording `template: <name>` provenance (read back as `fromTemplate` - the "Created from Weekly report template" audit string, now real) plus the template's declared `sources:` so the copied binds resolve on load; (c) writes + opens the file, `loadDocument`s it, then drives the SAME `sendChatMessage` path every chat generation uses with a composed brief from a second pure helper `composeTemplateInstruction` (template body as the brief + slot hints + the note) - so the model's output arrives as insertion proposals (decision 45) in the review rail (decision 17), never written prose. The History tab's origin row is now DATA-DRIVEN off `fromTemplate` (`reviewRailView.ts` `historyHtml`): a template-generated document shows a real "Created from &lt;name&gt; template" timeline row (with the source `&lt;name&gt;.template.md`), replacing the comp's static sample for that document - so the provenance line is honest end to end, not a mock. **No-model honesty:** with the proxy unreachable the skeleton is still created and bound, the doc status reads "Draft skeleton created - connect a model to fill it", and nothing is queued (no fabricated prose). Both helpers are pure + unit-tested (skeleton verbatim-binds/slot-strip/provenance, prose-only headings-only skeleton with comments stripped, snapshot `deepStrictEqual` on the composed brief); the service path is tested with a model (skeleton on disk + provenance + one insertion proposal + the brief drove the model call) and without (skeleton only, status explains, nothing queued). Settled to the plan's recommendation (unattended run). **Tier: our-surface, 0 core patches.**
