---
number: 9
status: "Done (api real, mcp stub)"
source: docs/07-decision-log.md
---

# The file, api and mcp source-kind abstraction

**`file | api | mcp` source-kind abstraction**; implement `api` for real, stub `mcp`

"Bound to an API/CRM" is the differentiator; api via `IRequestService` is demonstrable now, mcp needs a server
