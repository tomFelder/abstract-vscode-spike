---
number: 16
status: "Done; dev-only"
source: docs/07-decision-log.md
---

# Optional OpenRouter test backend in the proxy

**Optional OpenRouter test backend in the proxy**

Verify the live renderer->proxy->model->render flow against a cheap model without Anthropic credits; default path stays Anthropic OAuth, key never committed
