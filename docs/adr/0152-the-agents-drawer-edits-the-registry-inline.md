---
number: 152
status: "**Done (plan 32, iter 3).** Branch `32-orch-second-half`."
provenance: "plan 32 iter 3"
source: docs/07-decision-log.md
---

# The Agents drawer edits the registry inline

**The Agents-screen detail drawer builds on D32-B (decision 151): the read-only canvas strip stays, and the agent gains inline registry editing (a three-level policy `<select>` + a cron/heartbeat/event trigger picker), a full run log, create/duplicate/pause, and a cross-project "Run skill across project" strip - all persisted to `agents.json` and truthful**

Settled to the plan's iteration-3 recommendation. Pause is a `disabled?: boolean` flag on `IAgentDef` (default absent = enabled; only ever set true), which the scheduler (`runDueAgents`) and the event path (`onSourceChanged`) both skip, while a manual "Run now" is deliberately still honoured (explicit user intent). Policy stays exactly the three levels of the safety dial (spec 09 §4: `auto-figures` / `ask-before-apply` / `draft-only`); the trigger picker composes a cron `Day HH:MM`, a heartbeat `everyHours`, or an event source path (`*` = any). `createAgent`/`duplicateAgent`/`setAgentDisabled`/`setAgentPolicy`/`setAgentTrigger` live on the orchestrator (which owns the registry) and persist through the existing `agentStore` seam; the drawer's run log reads live from `getAgentRunsForAgent` (relative time, `via`, `N docs / N applied / N queued` outcome columns, a failure or still-running-skip line, and an "N queued" link into the review surface) with a truthful empty state ("No runs yet"). The cross-project skill run (the P3 gap) fans the single-doc grade over every living folder document via `runSkillAcrossProject` and renders one row per document with its real flag/pass verdict; a non-living doc or a model-less model-backed skill is honestly `skipped`, never a fabricated pass. **Tier: our-surface, 0 core patches.** Verified: 7 fake-clock orchestrator tests (create appends + persists a unique id; duplicate clones with a fresh id + no shared runs; a paused agent is skipped by the scheduler but runs manually; resuming re-admits it; a paused event agent stays quiet on a source change while the graph still flags dirty; policy set validates + rejects a bad value; a retargeted trigger fires at its new time) + `summariseSkillRun` (2 pure model tests) + `runSkillAcrossProject` (1 service test) + 8 render-harness tests (the canvas strip + the policy select with exactly three levels + the cron day/time + heartbeat-hours pickers + Save trigger; the run-log WHEN/VIA/OUTCOME columns + the "N queued" link; the failed-run + still-running-skip lines; the truthful empty run log; Duplicate/Pause/Resume + the paused note; the New-agent wiring + the PAUSED list chip; the cross-project skill affordance + the per-doc flag/pass strip with real tallies).
