---
number: 174
status: "**Decided.** Protocol text = plan 43 §5; master prompt = [plans/RUN-editor-v2-loop.md](plans/RUN-editor-v2-loop.md)."
provenance: "this session"
source: docs/07-decision-log.md
---

# The wave orchestration protocol

**Wave orchestration protocol: a Fable orchestrator that never implements + Opus 4.8 implementer sub-agents + independent Opus 4.8 adversarial validators, exchanging on OPEN PRs - the orchestrator opens a draft PR whose body is the loop's success-criteria checklist; the implementer pushes and posts before/after screenshots; ONLY the validator ticks boxes (with measured values + evidence paths, via getComputedStyle/getBoundingClientRect, never eyeballed); 3 fix rounds then park; squash-merge on PASS; pixel standard = exact hex, ±1px lengths, ramp-not-face type (plan 43 §3.6)**

Extends the RUN-p0-p1 three-role architecture with a PR-resident exchange so every loop is reviewable from the PR record alone, and makes "all boxes ticked by the validator" a mechanical merge gate. Sequencing: plan 44 alone first, then max 3 lanes with a file-ownership matrix (43 §4) instead of mid-flight negotiation.
