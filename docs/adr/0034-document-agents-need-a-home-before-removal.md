---
number: 34
status: "**OPEN (v4 iter 1).** Options: (a) relocate document agents to an on-demand affordance (e.g. a Review-tab disclosure or a per-doc menu) so the always-on Review content matches the comp while the capability survives; (b) accept the comp's de-emphasis and drop the panel (v1 surface loss). Recommend (a). Sequenced **after** D2 (source-peek) and D3 (toolbar) in the backlog."
provenance: "v4"
source: docs/07-decision-log.md
---

# Document Agents need a home before removal

**The comp's revised "Workbench v2" removes the right-rail Document-Agents panel and the toolbar "Run skill" entry — but document agents are shipped v1 functionality. Decide their home before deleting the panel.**

The new comp (`Living Documents - Workbench v2.dc.html`) drops the Skills tab (already done, decision 31) **and** the Document-Agents panel itself (Strategy/Financial/Formatting cards, RUN-ON-EXPORT, Add-skill), plus the editor's "Run skill" button. v3 folded that panel into the Review tab, where it still renders. Matching the comp means removing it from Review — but Run/Re-run/Apply-fix are wired v1 features, and with the Skills tab also gone they'd have no surface. The user constraint is "hold v1 functionality."
