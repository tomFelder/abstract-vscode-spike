# Team Notes

A plain Markdown file with **no** frontmatter and no data bindings. It should open in the
Living Document editor and render as formatted prose, not a blank page.

## This week

- Shipped the weekly summary refresh loop
- Reviewed the *meaning-change* approval flow
- Started on multi-document propagation

## Snippets

Inline `code` renders, and so do fenced blocks:

```ts
const greeting = "hello";
console.log(greeting);
```

> A blockquote, for good measure.

See the [Weekly Summary](./Weekly%20Summary.living.md) for the bound report.
